{
  "name": "calc-server",
  "version": "1.0.0",
  "description": "server for calculator",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/server.js"
  },
  "author": "asif sharif",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.19.2"
  },
  "dependencies": {
    "socket.io": "^2.2.0"
  }
}
